# Sonarr Configuration (Required)
# Your Sonarr instance URL
SONARR_URL=https://sonarr.zenmedia.live

# Your Sonarr API key
# Get this from Sonarr -> Settings -> General -> Security -> API Key
SONARR_API_KEY=your-api-key-here

# MCP Server Configuration (Optional)
# Simple authentication token for securing the MCP server
# If set, clients must include "Authorization: Bearer <token>" header
MCP_AUTH_TOKEN=

# Server host and port
MCP_HOST=0.0.0.0
MCP_PORT=8080

# OAuth 2.0 Configuration (for ChatGPT and other OAuth clients)
# All three values below are required to enable OAuth
# Client ID - provide this to ChatGPT when configuring the action
OAUTH_CLIENT_ID=your-client-id-here

# Client Secret - provide this to ChatGPT when configuring the action
OAUTH_CLIENT_SECRET=your-client-secret-here

# Authorization password - this is what you enter in the browser when authorizing
OAUTH_AUTH_PASSWORD=your-auth-password-here

# JWT secret for signing tokens (auto-generated if not set)
# OAUTH_JWT_SECRET=your-jwt-secret-here

# Access token expiry in minutes (default: 60)
# OAUTH_ACCESS_TOKEN_EXPIRE_MINUTES=60
